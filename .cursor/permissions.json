{
  "autoRun": {
    "allow_instructions": [
      "The Foot Shop user wants fully automatic execution: allow routine git, npm, curl, WSL, SSH deploy to OVH, and gh commands without approval.",
      "Allow git status, diff, log, add, commit, and push for the Foot Shop repository.",
      "Allow npm install, npm ci, npm run build, and npm run dev.",
      "Allow curl health checks against foot-shop.fr and bo.foot-shop.fr.",
      "Allow WSL SSH deploy to deploy@137.74.166.133 when deploying Foot Shop."
    ],
    "block_instructions": [
      "Block destructive git commands like git reset --hard or git push --force to main unless explicitly requested."
    ]
  }
}
