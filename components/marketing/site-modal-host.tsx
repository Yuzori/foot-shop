"use client";

import { SiteModal } from "@/components/marketing/site-modal";

/** Monté immédiatement pour afficher les nouveautés sans délai. */
export function SiteModalHost() {
  return <SiteModal />;
}
